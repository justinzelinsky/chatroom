# Chatroom

A simple chatroom built with React, Redux, and Socket.IO

## Setup & Running

### Install

`npm install`

### Run UI
`npm run dev` 

### Run Server

`node server.js`

and navigate to `localhost:8080`
